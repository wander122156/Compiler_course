+ —обрать все в один репозиторий

+ –азработчику из IEnvironment убрать addResult

+ { "num x ", [0] }, переделать на проверку вывода программы а не обращение к самому результату например num x; writeln(x) и провер€ть вывод

Read: считывать соответсвующий типу передаваемой переменно тип в Read
то есть если переменна€ типа nun то пытемс€ считать число, а если чтроку то как строку либо как вариант сделать ReadNum, ReadBool, Read String (не оченеь) 


¬опросы:

 ак сделать правильный вызов return (пока в Parser -> statements)?
    [ ]  
