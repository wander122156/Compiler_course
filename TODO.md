 описать втроенные функции в спеке
 не все операторы строк в спеке
 + вычисление по короткой схеме дл€ bool
 + сделать логические операции && || not
 + parseCondition нечетко описан в грамматике(убрать лишние правила по типу for_cond = expression)
 объ€снить выброс в тесте  func num f() { write(1) }