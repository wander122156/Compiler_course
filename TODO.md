+ Собрать все в один репозиторий

+ Разработчику из IEnvironment убрать addResult

+ { "num x ", [0] }, переделать на проверку вывода программы а не обращение к самому результату например num x; writeln(x) и проверять вывод

Read: считывать соответсвующий типу передаваемой переменно тип в Read
то есть если переменная типа nun то пытемся считать число, а если чтроку то как строку либо как вариант сделать ReadNum, ReadBool, Read String (не оченеь) 

